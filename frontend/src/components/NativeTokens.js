import React from 'react'
import axios from "axios";

function NativeTokens({ wallet, chain, nativeBalance, setNativeBalance, nativeValue, setNativeBalance }) {
  return (
    <div>NativeTokens</div>
  )
}

export default NativeTokens